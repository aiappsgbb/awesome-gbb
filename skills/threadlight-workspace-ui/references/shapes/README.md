# Workspace shape references — placeholder index

The `case-list/`, `inbox/`, `dashboard/`, `console/`, `kanban/`, and `map/`
shape directories are **canonization targets** — each gets a polished
vanilla HTML+CSS+JS reference once the corresponding pilot lands.

| Shape | Pilot that canonizes it | Notes |
|-------|-------------------------|-------|
| `case-list` | FSI KYC | The reference for case-managed flows |
| `inbox` | Retail returns triage (or PIM) | Chronological queue |
| `dashboard` | Mfg Supplier Risk | KPI tiles + drill-down |
| `console` | Telco Order Fallout | Live event stream + action toolbar |
| `kanban` | Telco Order Fallout (alternative) or FSI loan origination | Stage-based flow |
| `map` | Mfg Supplier Risk (alternative) or multi-region telco | Geographic |

Until canonized, each subfolder holds only its own README placeholder.
The parent SKILL.md defines the anatomy and visual rules so the canonization
work has clear acceptance criteria.
