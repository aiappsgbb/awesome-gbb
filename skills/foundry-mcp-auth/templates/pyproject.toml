[build-system]
requires = ["setuptools~=80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "foundry-mcp-auth-server"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp~=2.14.7",
    "mcp~=1.29.0",
    "PyJWT[crypto]~=2.10.1",
]

[tool.setuptools]
packages = []
