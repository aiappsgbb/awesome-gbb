[build-system]
requires = ["setuptools~=80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "foundry-mcp-auth-management"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "azure-ai-projects~=2.6.0",
    "azure-identity~=1.25.3",
    "azure-mgmt-resource~=23.1.0",
    "httpx[socks]~=0.28.1",
    "PySocks~=1.7.1",
]

[tool.setuptools]
packages = []
