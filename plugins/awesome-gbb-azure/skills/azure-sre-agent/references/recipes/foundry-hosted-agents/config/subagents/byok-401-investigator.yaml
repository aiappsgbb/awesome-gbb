metadata:
  name: byok-401-investigator
spec:
  instructions: subagents/byok-401-investigator.instructions.md
  handoffDescription: Investigates BYOK 401 errors from Foundry hosted agents — root cause is almost always the "Foundry User" RBAC role assigned at PROJECT scope but NOT at the underlying CognitiveServices ACCOUNT scope.
  handoffs: []
  tools:
  - RunAzCliReadCommands
  - QueryAppInsightsByAppId
  - SearchMemory
  agentType: Autonomous
  temperature: 0.1
  enableSkills: true
  allowedSkills:
  - hosted-agent-deploy-triage
