metadata:
  name: threadlight-runtime-investigator
spec:
  instructions: subagents/threadlight-runtime-investigator.instructions.md
  handoffDescription: Deep-dive on Threadlight runtime errors — ACA revision health, replica restarts, MAF/Foundry SDK errors, and the gap between SPEC § 11c declared retry policy vs observed behavior.
  handoffs: []
  tools:
  - RunAzCliReadCommands
  - QueryAppInsightsByAppId
  - QueryLogAnalyticsByWorkspaceId
  - SearchMemory
  agentType: Autonomous
  temperature: 0.1
  enableSkills: true
  allowedSkills:
  - threadlight-incident-triage
