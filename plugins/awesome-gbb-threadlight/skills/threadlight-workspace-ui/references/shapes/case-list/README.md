# `case-list` workspace shape — reference

> Status: 📋 Placeholder — to be canonized during the FSI KYC pilot.

The `case-list` shape is the default for case-managed processes. After the
KYC pilot, this folder should contain:

```
case-list/
├── index.html         # Polished vanilla HTML+CSS+JS reference
├── workspace.css      # Scoped styles
├── workspace.js       # Filter / detail / action toolbar logic
├── seed-loader.js     # Loads specs/sample-data/*.json
└── README.md          # How to map to React / Angular / Vue / Blazor
```

See parent SKILL.md for the anatomy and visual rules.
